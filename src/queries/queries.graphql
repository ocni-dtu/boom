query getStreams {
    streams {
        items {
            id
            name
        }
    }
}


query getUser {
    activeUser {
        id
        email
        name
        bio
        avatar
    }
}